{% set version = "0.14.1" %}
{% set sha256 = "17073d837b56551bede8f6befaeb4eba3d94335e590239842e3660f0763a091a" %}

package:
  name: scikit-build
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/s/scikit-build/scikit-build-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv
  noarch: python


requirements:
  host:
    - packaging
    - pip
    - python >=2.7
    - setuptools
    - setuptools_scm
    - wheel
  run:
    - cmake
    - distro
    - make
    - packaging
    - python >=2.7
    - setuptools
    - wheel

test:
  imports:
    - skbuild
  source_files:
    - tests/*
  commands:
    - pip check
  requires:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - cython
    - git
    - mock
    - path.py
    - pip
    - pytest
    - pytest-cov
    - pytest-mock
    - requests
    - setuptools
    - six

about:
  home: http://github.com/scikit-build/scikit-build
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Improved build system generator for CPython C extensions.
  doc_url: http://scikit-build.readthedocs.org
  dev_url: http://github.com/scikit-build/scikit-build

extra:
  recipe-maintainers:
    - isuruf
    - scopatz
    - jcfr
    - msarahan
    - henryiii
    - mayeut
